import {default as RESTAdapter} from './adapters/rest';
import {default as AuthenticatedRoute} from './authenticated-route';
import {default as capability} from './capability';

export default {
  capability,
  RESTAdapter,
  AuthenticatedRoute,
};
