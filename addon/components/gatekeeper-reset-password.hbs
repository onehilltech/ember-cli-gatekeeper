<MdcForm class="gatekeeper-reset-password" @submit={{this.submit}} @validity={{this.validity}} ...attributes
  {{did-insert this.didInsert}}>

  <MdcPasswordTextfield @style={{@style}}
                        @value={{this.password}}
                        @label={{unless @passwordPlaceholder this.passwordLabel}}
                        @errorMessage={{@passwordErrorMessage}}
                        placeholder={{@passwordPlaceholder}}
                        required="true" />

  <MdcButton type="submit"
             @label={{if this.submitting this.submittingButtonLabel this.submitButtonLabel}}
             @style={{@submitButtonStyle}}
             disabled={{this.submitButtonDisabled}} />
</MdcForm>