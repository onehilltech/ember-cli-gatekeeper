<MdcForm class="gatekeeper-forgot-password" @submit={{this.submit}} @validity={{this.validity}}>
  <MdcTextfield @style={{@style}}
                @value={{this.email}}
                type={{this.type}}
                @label={{unless @emailPlaceholder this.emailLabel}}
                @errorMessage={{this.emailErrorMessage}}
                placeholder={{@emailPlaceholder}}
                required="true"
                autocomplete="off" />

  <MdcButton type="submit"
             @label={{this.submitButtonLabel}}
             @style={{@submitButtonStyle}}
             disabled={{this.submitButtonDisabled}} />
</MdcForm>