import { module, test } from 'qunit';
import { TokenMetadata } from 'ember-cli-gatekeeper';

module ('lib | TokenMetadata', function (hooks) {
  test ('format relative dates correctly', function(assert) {
    console.log ('GREAT!!!!');
  });
});