import SignInComponent from 'ember-cli-gatekeeper/components/gatekeeper-sign-in';
import layout from '../templates/components/solid-sign-in';

export default SignInComponent.extend({
  layout,

  classNames: ['solid-sign-in']
});
